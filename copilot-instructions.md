# Copilot CLI Project Instructions

1. **Security:** Always use `is_safe_url` in `routes.py` for any redirects.
2. **Auth:** Ensure `post.author == current_user` for all Update/Delete actions.
3. **Cleanliness:** Do not suggest changes to the `venv/` or `env/` folders.